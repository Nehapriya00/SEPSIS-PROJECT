import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Sepsis Clinical Decision Support System',
  description: 'AI-powered clinical decision support for sepsis risk assessment',
  keywords: 'sepsis, clinical decision support, healthcare, AI, medical',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}